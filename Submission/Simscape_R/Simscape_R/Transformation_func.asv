function A = Transformation_func(theta,d,a,alpha)
A = [cosd(theta) -sind(theta)*cosd(alpha) sind(theta)*sin(alpha) a*cos(theta);
     sin(theta)  cos(theta)*cos(aplha) -cos(theta)*sin(alpha) a*sin(theta);
         0                sin(alpha)           cos(alpha)          d;
         0                    0                    0               1];
end
